# Models Directory

Trained model artifacts:
- `battery_soh_model.pkl`: Battery state-of-health regressor (RandomForest / GradientBoosting).
- `battery_cycle_model.pkl`: Remaining useful cycles estimator.
- `valuation_model.pkl`: Fair-market price valuation regressor.
